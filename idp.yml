version: '3.6'
services:
  idp:
    container_name: idp
    restart: always
    image: ${IMAGE}
    networks:
      - dc-net
    environment:
      CONFIG_SERVER_URL: http://config_server
      SERVICE_NAME: idp
      NAMESPACE: prod
      FALLBACK_REDIRECT: ${OAUTH_FALLBACK_REDIRECT-/}
#    ports:
#      - 82:81
networks:
  dc-net:
    external: true